import React from "react";
import { render } from "@testing-library/react";
import Cell from "./Cell";

describe("Cell component", () => {
  // it('should render a cell with the "live" class when healthy is true', () => {
  //   const { container } = render(<Cell x={0} y={0} healthy={true} />);
  //   const cell = container.querySelector(".Cell.live");
  //   expect(cell).toBeInTheDocument();
  // });

  // it('should render a cell with the "death" class when healthy is false', () => {
  //   const { container } = render(<Cell x={0} y={0} healthy={false} />);
  //   const cell = container.querySelector(".Cell.death");
  //   expect(cell).toBeInTheDocument();
  // });

})